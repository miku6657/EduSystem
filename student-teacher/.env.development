# 开发环境配置（本文件必须提交，否则 README/接口手册里的开关无处可改）
# axios 基础路径（配合 vite 代理）
VITE_API_BASE_URL=/api
# Vite 代理目标：本机 Spring Boot 后端
VITE_PROXY_TARGET=http://localhost:8080
# 是否启用本地 mock（true=用 mock 演示，false=直连后端）
VITE_USE_MOCK=true
# 页面标题
VITE_APP_TITLE=教学过程管理系统 · 师生端
